How to use:
1) Set Cloudflare Pages Environment variables (Production + Preview):
   - GITHUB_CLIENT_ID
   - GITHUB_CLIENT_SECRET
2) GitHub OAuth App callback: https://<project>.pages.dev/api/callback
3) Open /admin to login.
